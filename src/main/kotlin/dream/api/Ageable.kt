package dream.api

/**
 * The Ageable interface represents an object that can have an age and a maximum age.
 */
interface Ageable {
  
  /**
   * The current age of the object.
   */
  var age: Int
  
  /**
   * The maximum age that the object can reach.
   */
  var maxAge: Int
}
