package dream.item

/**
 * Represents all item flags that's can be showed in
 * tooltip when hovering an item.
 */
enum class HideFlag {
   ENCHANTMENTS,
   ATTRIBUTES,
   UNBREAKABLE,
   CAN_DESTROY,
   CAN_PLACE,
   POTION_EFFECT
}
